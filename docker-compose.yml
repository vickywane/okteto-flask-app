version: '2'
services:
  database:
    image: docker.io/bitnami/couchdb:3
    environment:
      - COUCHDB_USER=couchdb-admin
      - COUCHDB_PASSWORD=couchdb-password
    ports:
      - '5984:5984'
    extra_hosts:
      - "dockerhost:0.0.0.0"
    volumes:
      - couchdb_data:/bitnami/couchdb

#  api:
#    depends_on:
#      - database
#    build:
#      target: bash
#      context: .
#    command: ping http://dockerhost:5984
#
#  app:
#    depends_on:
#      - api
#    build:
#      target: python
#      context: .
#    environment:
#      - FLASK_RUN_HOST=0.0.0.0
#      - FLASK_RUN_PORT=5050
#    command: flask run
#    ports:
#      - '5050:5050'

volumes:
  couchdb_data:
    driver: local
